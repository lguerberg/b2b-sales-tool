export type CampaignColumn = {
  name: string
  description: string
  status: string
  groupName: string
  leads: number
}

export type CampaingEmailColumn = {
  email: string
  subject: string
  body: string
  status: string
}
