{
  "name": "silverdev-challenge",
  "version": "1.0.0",
  "description": "Silver Dev Challenge - B2B Sales Tool from Leonardo Guerberg.",
  "main": "index.js",
  "scripts": {
    "all:dev": "pnpm run --parallel dev",
    "api:dev": "docker-compose -f docker/docker-compose.yml up -d",
    "api:down": "docker-compose -f docker/docker-compose.yml down",
    "format": "pnpm run --parallel format",
    "lint:fix": "pnpm run --parallel lint:fix",
    "dep:upgrade": "manypkg upgrade"
  },
  "devDependencies": {
    "@manypkg/cli": "^0.21.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
